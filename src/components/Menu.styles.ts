import { type CSSObject } from "@mui/material/styles"

export const MenuIconButtonStyles: CSSObject = {
    color: "white",
    left: 8,
    position: "absolute",
    top: 8,
}

export const MenuListStyles: CSSObject = {
    "& > .songs-list > .MuiListItemButton-root": {
        paddingLeft: 32,
    },
}
